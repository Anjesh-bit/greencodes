import { ForImageCarousalPipe } from './for-image-carousal.pipe';

describe('ForImageCarousalPipe', () => {
  it('create an instance', () => {
    const pipe = new ForImageCarousalPipe();
    expect(pipe).toBeTruthy();
  });
});
