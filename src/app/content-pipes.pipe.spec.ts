import { ContentPipesPipe } from './content-pipes.pipe';

describe('ContentPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
